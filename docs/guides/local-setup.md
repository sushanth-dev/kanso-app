# Local setup

The application code has not been written yet, so there is nothing to run.
This guide will be filled in with the first service. Expect it to cover:

* Prerequisites: Node.js 26 and a local PostgreSQL instance.
* Installing dependencies with `npm ci`.
* Copying `.env.example` to `.env` and filling in local values.
* Running the development servers for the backend and the Vite frontend.
* Running the test suites: Vitest for unit and integration, Playwright for
  end to end.
