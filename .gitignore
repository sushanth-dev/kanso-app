# Dependencies
node_modules/

# Builds and caches
dist/
build/
*.tsbuildinfo
.vite/
.turbo/

# SST's local state and generated types. The deploy state itself lives in an
# S3 bucket SST creates, not here.
.sst/
sst-env.d.ts

# Environment files; secrets never live in the repo (see docs/CONTRIBUTING.md)
.env
.env.*
!.env.example

# Test and coverage output
coverage/
playwright-report/
test-results/

# Logs
*.log

# Databases
*.sqlite
*.sqlite3

# OS and editors
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Docs site build
site/

# Style Dictionary build output; tokens/ sources are committed, generated files are not
tokens/_computed.json
tokens/build/
apps/web/src/generated/
apps/web/playwright-report/
apps/web/test-results/

# graphify knowledge graph output; regenerated from the repo, not a source of record
graphify-out/
